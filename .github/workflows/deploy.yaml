name: Deploy Snake Game to AWS

on:
  push:
    branches: [ main ]

env:
  AWS_REGION: us-east-1

jobs:
  terraform:
    name: Terraform Infrastructure
    runs-on: ubuntu-latest
    environment: production
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Terraform Init
      run: |
        cd terraform
        terraform init

    - name: Terraform Apply
      run: |
        cd terraform
        terraform apply -auto-approve -var="db_password=${{ secrets.DB_PASSWORD }}"

    - name: Get ECR repository URL
      id: get-ecr-url
      run: |
        cd terraform
        ECR_URL=$(terraform output -raw ecr_repository_url)
        echo "ECR_URL=$ECR_URL" >> $GITHUB_OUTPUT
        echo "ECR repository URL: $ECR_URL"

    - name: Get ECS task execution role ARN
      id: get-role-arn
      run: |
        cd terraform
        TASK_EXEC_ROLE_ARN=$(terraform output -raw ecs_task_execution_role_arn)
        echo "TASK_EXEC_ROLE_ARN=$TASK_EXEC_ROLE_ARN" >> $GITHUB_OUTPUT
        echo "Task execution role ARN: $TASK_EXEC_ROLE_ARN"

  deploy-app:
    name: Deploy Application
    runs-on: ubuntu-latest
    needs: terraform
    environment: production

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v2

    - name: Build and push Docker image
      run: |
        cd src
        # 先构建镜像
        docker build -t snake-game:latest .
        
        # 然后使用正确的ECR URL进行标记和推送
        ECR_URL="${{ needs.terraform.outputs.ECR_URL }}"
        echo "Pushing to ECR: $ECR_URL:latest"
        
        docker tag snake-game:latest $ECR_URL:latest
        docker push $ECR_URL:latest

    - name: Update task definition with correct values
      run: |
        # 使用正确的角色ARN和镜像URL更新任务定义
        ECR_URL="${{ needs.terraform.outputs.ECR_URL }}"
        ROLE_ARN="${{ needs.terraform.outputs.TASK_EXEC_ROLE_ARN }}"
        
        echo "Using ECR URL: $ECR_URL"
        echo "Using Role ARN: $ROLE_ARN"
        
        jq \
          --arg image "$ECR_URL:latest" \
          --arg role_arn "$ROLE_ARN" \
          '.containerDefinitions[0].image = $image | .executionRoleArn = $role_arn' \
          task-definition.json > updated-task-definition.json
        
        echo "Updated task definition created"

    - name: Register ECS task definition
      run: |
        aws ecs register-task-definition --cli-input-json file://updated-task-definition.json

    - name: Update ECS service
      run: |
        aws ecs update-service \
          --cluster snake-game-production-cluster \
          --service snake-game-service \
          --task-definition snake-game-task \
          --force-new-deployment

    - name: Wait for deployment
      run: |
        echo "Waiting for deployment to complete..."
        sleep 30
        aws ecs wait services-stable \
          --cluster snake-game-production-cluster \
          --services snake-game-service

    - name: Health check
      run: |
        # 获取负载均衡器DNS
        LOAD_BALANCER_DNS=$(aws elbv2 describe-load-balancers \
          --query 'LoadBalancers[?LoadBalancerName==`snake-game-production-alb`].DNSName' \
          --output text)
        
        echo "Application URL: http://$LOAD_BALANCER_DNS"
        echo "Testing health endpoint..."
        
        # 健康检查
        for i in {1..10}; do
          if curl -f -s http://$LOAD_BALANCER_DNS/health > /dev/null; then
            echo "✅ Health check passed!"
            break
          else
            echo "⏳ Health check attempt $i failed, retrying in 10 seconds..."
            sleep 10
          fi
        done

    - name: Deployment Summary
      run: |
        LOAD_BALANCER_DNS=$(aws elbv2 describe-load-balancers \
          --query 'LoadBalancers[?LoadBalancerName==`snake-game-production-alb`].DNSName' \
          --output text)
        
        echo "🎉 Deployment Completed Successfully!"
        echo "========================================"
        echo "🌐 Application URL: http://$LOAD_BALANCER_DNS"
        echo "🛠️  Health Check: http://$LOAD_BALANCER_DNS/health"
        echo "🐳 ECR Image: ${{ needs.terraform.outputs.ECR_URL }}:latest"
        echo "⚓ ECS Cluster: snake-game-production-cluster"
        echo "🔧 ECS Service: snake-game-service"